# Structured-Shift Diagnostics

The smoke and parity runs were used to validate the structured directional
observation-shift implementation before the final five-seed experiment.

The parity run confirmed exact agreement of the clean baseline with the frozen
Gaussian experiment when the same 1,000 held-out queries were used.

These diagnostic artifacts are retained for reproducibility and are not part
of the primary inferential result.
